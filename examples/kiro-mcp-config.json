{
  "mcpServers": {
    "filesystem": {
      "command": "mcpfw",
      "args": ["--policy", "policies/standard.yaml", "-l", "mcpfw-audit.jsonl",
               "--", "npx", "-y", "@modelcontextprotocol/server-filesystem", "."]
    }
  }
}
